# Shared HTTP-level controls for the public RobVoto applications.
server_tokens off;

# Allow normal browser bursts while limiting sustained automated traffic per IP.
limit_req_zone $binary_remote_addr zone=robvoto_per_ip:10m rate=10r/s;
limit_req_status 429;
