#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")"
export UV_CACHE_DIR="${UV_CACHE_DIR:-/tmp/job-hunter-agent-uv-cache}"
mkdir -p "$UV_CACHE_DIR"
exec uv run python -m job_hunter_agent.knowledge_sync "$@"
